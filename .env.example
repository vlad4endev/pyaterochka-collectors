# PostgreSQL
DATABASE_URL="postgresql://pyaterochka:pyaterochka@localhost:5432/pyaterochka?schema=public"

# Admin login (not stored in the database)
ADMIN_PASSWORD="changeme"

# API
PORT=3001

# Telegram bot + Mini App
# Token from @BotFather. Leave empty to run API/admin without the bot.
BOT_TOKEN=""
# Public HTTPS URL of the Vite app (same origin as the Mini App).
# Locally: Cloudflare Tunnel / ngrok to http://localhost:5173
# In @BotFather: Bot Settings → Menu Button → this same URL
MINIAPP_URL=""
